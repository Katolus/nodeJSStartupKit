// ES5
// var chalk = require("chalk");
// Library that allows to specify the color of the output that we are displaying to the console

//ES6 standard
import chalk from 'chalk';

console.log(chalk.green("Server is starting..."));
